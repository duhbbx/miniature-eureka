# miniature-eureka




嘿嘿，记录一下日常学习吧


## 设计模式

- desing-pattern: 设计模式




## 经验

1. 整数相关的反转操作可能导致溢出，用 `long long` 或则想办法避免溢出
2. `int` 类型的负数和 `unsigned long` 类型的数比较大小需要注意
3. `string.length()` 的类型是 `unsigned long`
4. `for` 循环中如果代码块中有对指标的自增，则需要注意 `for` 循环块中的 `i++`
5. 整数反转，相加，相减等如何避免溢出或者知道溢出呢，`-2^31` 和 `2^31-1` 在 C++ 中各是多少
6. C++ lambda 表达式怎么用？排序中的 compare 如何用 lambda 呢？ 
